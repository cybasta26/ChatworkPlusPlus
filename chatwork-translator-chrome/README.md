> v1.12.2：修复含 Logic2 等未列入白名单的英文名称时，明显中文被漏判的问题。

- 中文判断使用汉字占比（至少 80%）与中文用语证据，不再因为少量未知英文名称直接否决；英文名称仅影响语言判定，原文不会被替换或改写。
- 对明确中文，不依赖浏览器检测器识别成功；检测接口不存在、不可靠或错误偏向英语时仍能识别。日文假名、韩文、英文占比较高的混合文本和简繁体差异仍有保护。
- 截图原句回归测试已移除模拟“中文识别成功”的条件；验证第 2 行中文原样保留，下面三行日文继续送入模型，换行不变。
- 更新后重新加载扩展并刷新 Chatwork。Edge 保留 v1.12.1 的异常会话恢复与逐行失败标注。

> v1.12.0：自动翻译前识别整条消息的主要语言；如果已经是所选目标语言，保留原文且不显示重复译文。

- 按当前保存的目标语言判断，不按浏览器语言硬编码；支持简体/繁体中文、日语、英语、韩语，及浏览器检测器能可靠识别的其他语言。
- 优先使用扩展内置的 `chrome.i18n.detectLanguage`，要求结果可靠、主语言占比至少 80%。姓名保护标记、链接、TO/RE/CC、常见产品名不参与判断；原文不会被修改。
- 检测器不可用、失败或超时时，对明显的中/日/英/韩正文使用保守的文字特征判断；不明确的短句或混合语言继续翻译，避免漏译。语言识别不是百分之百准确。
- 简体与繁体保留区别：发现与目标不同的常见字形时仍翻译，不会直接跳过需要转换的文字。
- 已是目标语言的消息不调用 Google 或 Edge 翻译模型，也不显示空的翻译框；检测不新增网络翻译请求、模型下载或扩展权限。
- 此规则用于聊天自动翻译，手动翻译仍按用户选择执行。原来的换行、姓名/链接保护、首次安装默认值和用户保存的设置保持不变。

> v1.11.0：首次安装默认“日语 → 浏览器界面语言”；日语环境默认关闭自动翻译，其他语言默认开启。

- 简体/繁体中文、英语、韩语环境分别以对应语言为目标；其他浏览器语言也会按语言代码传给翻译服务。界面仍支持已有五种语言。
- 初始化后保存默认值。以后更改浏览器语言不会覆盖已保存的开关/语言；旧版本升级保留已有显式设置和原先隐含的中文目标语言。
- 自动翻译打开时，面板显示可调整的原文/译文语言；Edge 模型设置页也可选择目标语言。关闭时隐藏语言设置。
- 日语环境默认方向为日语→日语且开关关闭；若需要翻译其他语言，开启后选择原文语言。原文语言与目标相同时直接跳过，不调用模型。
- Edge 仍使用本地模型，所选语言对需浏览器支持并下载对应模型；不支持时明确显示提示。Chrome 将实际语言对传给 Google。
- 手动翻译和成员导航的开关、语言、设置保持原样。为避免误导，界面名称改为“Chatwork 翻译”。
- 本版本新增 Edge 后台初始化脚本，但没有新增权限。重新加载扩展并刷新 Chatwork 后生效；已有安装不会自动改成首次安装的默认值。

> v1.10.1：自动翻译改为与其他功能一致的拨动开关；Edge 的模型/语言设置按钮仅在自动翻译开启时显示。保留已有开关设置和五语言界面适配。

> v1.10.0：界面自动跟随浏览器语言，支持简体中文、繁体中文、英语、日语、韩语；其他语言回退英语。

- 使用浏览器原生 `chrome.i18n` 和 `_locales`，覆盖插件名称/简介、面板、手动翻译控件、导航/预览、状态/错误提示，以及 Edge 模型设置页。
- 本次只适配界面。聊天自动翻译仍输出中文，手动翻译源语言/目标语言、已保存的开关和成员导航设置均保持不变。
- 使用浏览器的界面语言，不跟随 Chatwork 网页语言。修改浏览器显示语言后按浏览器提示重启，再刷新 Chatwork 页面。
- 升级后在扩展管理页重新加载此扩展，再刷新 Chatwork；已有设置无需重置。没有新增权限，也不会为界面文案调用任何翻译服务。
- 扩展商店的各语言介绍和截图需要在商店后台单独填写，本次尚未提交商店。

> v1.9.1：消息旁恢复“相关信息 2/4”形式；修复未加载消息点击箭头却弹出 Chatwork 原生预览的问题。

- ↑↓ 始终用于直接定位。目标已加载时滚动并高亮，未加载时进入 Chatwork 消息直达链接，加载后高亮，不再点击原生 RE 预览入口。
- “预览”按钮仍只弹窗；小窗内“前往此消息”也使用同一直接定位逻辑。受权限限制未能加载目标时会在顶部状态提示定位失败。
- 顶部“相关信息：总数”保持不变，消息旁则显示当前序号/关联总数。
- 本条替代旧版对原生回复入口及仅显示关联数量的描述。

> v1.9.0：消息内新增“相关信息：数量 ↑ 预览 ↓ 预览”。点击预览弹出相邻关联消息的小窗，箭头继续直接跳转。

- 小窗显示已加载消息的发送者、时间、原文和已有译文；长内容在小窗内滚动，底部“前往此消息”定位原聊天。
- 点击预览不改变聊天位置，不额外调用翻译服务。未加载的原消息显示提示，可通过“前往此消息”调用 Chatwork 已有回复入口；仍受历史记录权限限制。
- 点击 ×、空白处、按 Esc、滚动聊天室或关闭导航可关闭预览。小窗内滚动不会关闭。每次只打开一个预览。
- 无关联时保持“无人回复”，箭头和预览按钮一并隐藏。

> v1.8.2：顶部改为“相关信息：xx”，显示所选成员的 TO、RE、SEND 消息合并去重后的总数。消息内没有已加载关联时显示“无人回复”并隐藏上下箭头，加载到关联后恢复导航。

“无人回复”表示当前已加载记录中没有明确关联，并不代表尚未加载的历史记录中也没有回复；悬停文字可查看说明。本条替代旧版本中的分类计数和“暂无已加载关联”显示规则。

> v1.8.1：修复滚动后相关信息入口缺失或使用旧消息编号的问题。

- 普通消息均保留“相关信息”入口。当前网页尚未加载其明确回复关系时，显示“暂无已加载关联”，上下箭头置灰；有相关回复加载后自动更新。
- 监听滚动及消息编号变化，支持页面复用消息节点、重建发送者栏后补回按钮；持续页面变动也不会无限推迟更新。
- 仍只按明确 RE 回复链关联；相同 TO 对象不会被当作同一话题。可用顶部刷新补充历史记录。
- 本条替代 v1.8.0 中只为有回复关系的消息显示入口的规则。

> v1.8.0：TO / RE / SEND 数量合并为一个只读文本框；顶部箭头在三类消息的去重合集里跳转。消息内新增“↑ 相关信息 ↓”。

## 统一跳转与相关信息（当前规则）

- 顶部显示 `TO: 5   RE: 10   SEND: 5`，只显示计数，无需选择分类。上下箭头按消息顺序循环跳转所有匹配消息；一条消息命中多个分类仍只经过一次，分类数量各自保留。
- 有回复关系的消息，在正文上方显示“↑ 相关信息 序号/总数 ↓”。按同一聊天室的明确 RE 回复链连接，包括连续回复与分支；不根据相同人名、TO 对象或相似文字猜测关联。
- 消息内的箭头沿关联对话前后跳转，到两端时禁用，不循环。关联范围不受顶部所选成员和天数限制，但只知道网页已加载的回复关系，以及这些消息明确指向的原消息。
- 原消息尚未加载时，优先通过 Chatwork 原有 RE 回复入口定位；账号权限、消息删除等限制仍可能使定位失败。更多尚未加载的后续回复需要先用顶部刷新加载历史。
- 关闭插件面板里的“成员消息导航”，同时隐藏顶部导航和消息内相关信息按钮，不影响自动翻译。
- 以下版本中的可点击 TO/RE/SEND 分类描述已由本版替代。

> v1.7.0：成员选择 → 最近天数 → 刷新 → TO / RE / SEND → 统一上下箭头；插件面板新增“成员消息导航”开关，默认开启。

## 成员消息导航（当前规则）

- 左侧默认选择自己，也可切换到网页已加载的其他成员；切换聊天室恢复为自己。下拉列表尚未出现的成员，可以先展开 Chatwork 成员列表，或选择“输入其他账号编号”。自己的账号未识别时，可选“设置自己的账号”。
- 默认最近 3 天，可修改。点击 ⟳ 刷新并加载历史记录；加载期间按钮变为 ■，点击可停止。
- TO 是发给所选成员的 TO 消息，RE 是回复所选成员的消息，SEND 是该成员发出的消息。点击 TO / RE / SEND 选中分类，再用右侧 ↑↓ 循环跳转；旁边显示当前序号/总数。
- 数量按消息去重，不统计引用块中的 TO/RE、TOALL 和已删除消息。SEND 根据消息发送者的账号编号判断，不使用正文中的人名。
- 数量旁的橙色圆点表示仅统计已加载记录；绿色勾表示已覆盖指定时间范围。悬停可查看详细状态。历史权限和网络限制依然适用。
- 插件面板里的“成员消息导航”可独立开关，设置保存后立即生效；关闭会隐藏导航并取消正在进行的加载，不影响翻译开关。
- 以下 v1.6.0 的“我”按钮和每类各有上下箭头的描述已由本版替代。

> v1.6.0：聊天室顶部增加 TO 我 / RE 我统计和上下跳转，默认最近 3 天。

## 发给我的消息导航

- 在当前聊天室标题旁显示 `TO 我 数量 ↑ ↓`、`RE 我 数量 ↑ ↓`，箭头循环跳转并高亮对应消息；跳转后显示当前位置/总数。
- “近 N 天”按最近 N×24 小时计算，默认 3 天，可设 1–365 天并保存。修改天数后点击“加载”补齐历史。
- 数量按消息去重；同一条消息同时 TO 和 RE 自己时，两类各计一次。只计算直接指向自己账号编号的 TO/RE，引用块、TOALL、已删除消息不计入。
- 自动用登录菜单的姓名和头像匹配页面上的账号编号。无法识别时，点击“我”选择自己的姓名或填写数字账号编号；设置按当前登录账号保存。
- 初始显示“仅已加载”；点击“加载”后先定位最新消息，再分批向上加载，直到跨过日期起点且消息索引连续，显示“已覆盖时间范围”。完成后恢复原阅读位置。
- 加载期间暂停新增自动翻译任务。点“停止”、滚动鼠标、触屏或按方向/翻页/Esc 键可中断；切换聊天室也会取消。
- 无法继续加载时明确显示“仅已加载”，不把部分记录当作完整数量。受 Chatwork 账号历史权限、网络与网页加载机制限制，不能保证任意 N 天都能取齐。每轮最多向上加载 120 批，可再点加载继续。
- 聊天内容不额外保存，也不使用私有接口或要求 API Token。窄窗口中工具栏会移至标题下方，避免遮挡聊天室原有按钮。
- 更新：到浏览器扩展管理页重新加载本扩展，再刷新 Chatwork。Chrome 和 Edge 两版功能一致。

> v1.5.1：自动翻译和手动翻译默认开启（未保存设置时生效）；用户明确关闭的设置仍保留。更新已有安装不会强行覆盖关闭状态。Edge 模型尚未就绪时仍需初始化。

# Chatwork 中文翻译 · Chrome Google 版 v1.5.0

与 Edge v1.5.0 同步界面和文字处理规则，翻译服务仍为 Google，无需下载 Edge 模型。

## 安装与更新

1. Chrome 打开 chrome://extensions，开启「开发者模式」。
2. 「加载已解压的扩展程序」，选择 F:\Projects\DCG\DCG_Doc\ChatworkTranslator\chatwork-translator-chrome。
3. 已安装的用户点击该扩展「重新加载」，再刷新 Chatwork。
4. 打开插件面板，开启自动翻译。分享 ZIP 时先解压到固定文件夹，再加载。

## 同步功能

- 原文下显示译文，新消息、历史加载、切换聊天室、正文编辑自动处理；仅处理进入视野的消息。
- 所有实际换行和空行原样保留，逐行完整翻译。数字、时间、英文不从同一行里拆开。自动折行不是原文换行；跨行句子的上下文会减少。
- 可识别人名、TO:/RE:、链接、重复分隔线与装饰符号保留。模型改写保护标记时尝试恢复，失败后自动对该行分段回退。
- 明确为中文的整条聊天消息跳过，不显示重复译文框。保守本地判断，不确定的短句仍可能翻译。
- 独立手动翻译拨动开关，关闭隐藏输入输出；默认中文→日语，可选择中文、日语、英语、韩语。支持 Ctrl+Enter 和复制译文。
- 手动翻译独立于聊天开关，中文输入照常可翻译到其他语言。输入与译文不持久保存，关闭面板清空；开关、语言选择会保存。
- 紧凑面板同时显示输入和输出，没有整页滚动条；长文字在文本框内滚动。

## 网络与数据

开启自动翻译或点击手动翻译会将相应正文发送给 Google，包括未识别的人名及正文引用。附件不下载、不翻译。插件不发送 Chatwork 消息，不改输入框。

使用 translate.googleapis.com 的免密钥接口，需要网络能连接 Google，可能限流或变更，没有可用性保证。不开通付费 API。翻译结果仅在后台内存缓存最多 300 个请求，缓存按源语言和目标语言区分。

聊天翻译源语言自动检测，目标为简体中文；手动翻译使用所选语言。暂停后不启动新的请求，已发出的网络请求可能完成。

## 验证

npm install 后运行 npm test。14 项自动化测试覆盖动态消息、编辑、暂停、手动翻译、语言方向、过期结果、换行、中文跳过、姓名与链接保护、标记回退、限流等。未在用户真实 Chrome 扩展环境进行安装后端到端验证。

本次实际网络测试：使用公开示例句请求 Google，返回 HTTP 429（限流）；未取得实际译文。需待限流解除后在 Chrome 中验证。

